version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region ca-central-1 | docker login --username AWS --password-stdin 504151741135.dkr.ecr.ca-central-1.amazonaws.com
  build:
    commands:
      - echo Build started on `date`
      - docker build -t abraham-app .
      - docker tag abraham-app:latest 504151741135.dkr.ecr.ca-central-1.amazonaws.com/abraham-app:latest
      - docker push 504151741135.dkr.ecr.ca-central-1.amazonaws.com/abraham-app:latest
  post_build:
    commands:
      - echo Build completed successfully
